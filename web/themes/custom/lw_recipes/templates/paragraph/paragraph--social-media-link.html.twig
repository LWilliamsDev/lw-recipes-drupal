{% set icon = paragraph.getSocialMediaIcon() %}
{% set alt = content.social_media_site %}
{% set link = paragraph.getUrl() %}

{% if icon and link %}
  <li>
    <a href="{{ link }}" target="_blank" class="block"><img src="{{ icon }}" alt="{{ alt }}" class="p-[12px]"></a>
  </li>
{% endif %}

