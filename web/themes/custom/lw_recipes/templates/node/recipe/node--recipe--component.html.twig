{% set image = content.hero_image %}
{% set excerpt = content.excerpt %}
{% set diets = content.diet %}

<div class="card mb-8">
	<a href="{{ url }}">
        {{ image }}
        {#<img src="assets/img/home/cake-pops.jpg" alt="Cake Pops" class="mb-5 w-full h-auto rounded-lg" />#}
    </a>
	<h3 class="text-2xl font-medium text-(--color-brown)"><a href="{{ url }}">{{ node.title }}</a></h3>
		<p class="text-(--color-dark-green)">{{ excerpt }}</p>
			<ul class="categories mt-5 flex flex-wrap gap-[10px]">
                {% for diet in diets %}
				<li>
                    {# figure this out #}
                    {#<a href="#" class="button p-[10px] inline-block rounded-sm text-(--color-white) font-medium">
                        Paleo
                    </a>#}
                </li>
				{% endfor %}
			</ul>
</div>